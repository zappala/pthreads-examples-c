======================================================================
Example code for threading
======================================================================

Contents:

1) Makefile - Will compile the code and keep track of dependencies, so
that the code is recompiled correctly when any of the source code is
modified.

2) test.cc - Creates two threads to print out different strings.  Shows
how to pass arguments to a thread when it is created.

Syntax:

./test
